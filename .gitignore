docs/
CLAUDE.md
AGENTS.md

.env

legal/

node_modules/
dist/
coverage/
.cache/
*.tsbuildinfo

*.log
.DS_Store

creva-report.html
creva-report.json

# Máster de la grabación de pantalla: 96,9 MB. GitHub avisa arriba de 50 MB y
# rechaza arriba de 100. Lo que se publica son los recortes de `media/`, 605 KB.
creva-score.mp4

# Skills vendored by `npx skills add`; skills-lock.json is what pins them.
.agents/
.claude/

# Llaves de firma. La privada nunca se commitea; la pública se publica
# deliberadamente en una URL estable, no por accidente desde una llave de prueba.
*.key
*.key.pub
creva-signing.key
